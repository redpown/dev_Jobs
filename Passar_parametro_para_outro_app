
codgo no progama que ira ralizar a chamada
private void button1_Click(object sender, EventArgs e)
        {
            string exe = @"D:\projetos\C# net core\memory scanner net core 6\WindowsFormsApp2\bin\Debug\WindowsFormsApp2.exe";
            //os parametros são passados por espaço
            Process.Start(exe, textBox1.Text +" "+textBox2.Text);
        }
        
no programa que será chamado

 [STAThread]
        static void Main(string[] args)
        {
            string parametro1 = "";
            string parametro2 = "";
            if (args.Length > 0) {
                 parametro1 = args[0];
                 parametro2 = args[1];
            }
           

            Application.EnableVisualStyles();
            Application.SetCompatibleTextRenderingDefault(false);
            Application.Run(new Form1(parametro1, parametro2));
        }
